# Exclude Rust build artifacts to speed up Docker builds
# This reduces context transfer from ~15GB to ~200MB
target/

# Exclude other build artifacts
**/*.rs.bk
**/*.pdb

# Exclude Git files
.git/
.gitignore

# Exclude IDE files
.vscode/
.idea/
*.swp
*.swo
*~

# Exclude test artifacts
*.log
*.profraw
*.profdata

# Exclude macOS files
.DS_Store

# Exclude documentation (not needed for Docker build)
docs/

# Exclude benches (not needed for Docker build)
benches/

# Don't exclude tests/ - they are workspace members needed for build
